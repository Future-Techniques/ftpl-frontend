import React from 'react';

const Helper = ({ helperclass }) => {
      return (
            <>
                  <span className={helperclass}></span>
            </>
      );
};

export default Helper;
